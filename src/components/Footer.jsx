import styled from "styled-components";

const StyledFooter = styled.footer`
  color: red;
`;

function Footer() {
  return (
    <StyledFooter>
      <h1>footer</h1>
    </StyledFooter>
  );
}

export default Footer;
